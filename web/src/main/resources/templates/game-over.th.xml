<thlogic>
    <attr sel="#headId"
          th:replace="~{fragments/head :: headFragment}"/>
    <attr sel="#result-message"
          th:text="${__${T(academy.learnprogramming.util.AttributeNames).RESULT_MESSAGE}__}"/>
    <attr sel="#play-again"
          th:action="@{__${T(academy.learnprogramming.util.GameMappings).RESTART}__}"/>
    <attr sel="#home"
          th:action="@{__${T(academy.learnprogramming.util.GameMappings).HOME}__}"/>
    <attr sel="#main-message"
          th:text="${__${T(academy.learnprogramming.util.AttributeNames).MAIN_MESSAGE}__}"/>
    <attr sel="#footerId"
          th:replace="~{fragments/footer :: footer}"/>
</thlogic>